import * as Styled from './styles';
import {BaseTemplate } from '../Base';
import { useRouter } from 'next/router'


export const AguardeTemplate = () => {
  return (
      <Styled.ContentContainer>
        <h1>Aguarde....</h1>
      </Styled.ContentContainer>
  );
};
